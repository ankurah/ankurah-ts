// MIRRORS: ankurah/core/src/util/cast.rs
//
// Rust provides `into!` and `create!` macros for ergonomic entity creation
// with automatic `.into()` on fields. These are compile-time macros with no
// TS equivalent. Entity creation in TS uses defineModel() generated constructors
// instead. This file is intentionally empty — kept as a placeholder to mirror
// the Rust module structure.
