# Diagnostics budget, written by transpile/tests/diagnostics_budget.rs.
# Each number is how many fallbacks the translator still takes in that
# crate — how much of it the engine cannot yet type. Every number is
# matched EXACTLY, in both directions: a rise is coverage lost, and a
# fall is coverage gained that has to be recorded, so a step that stops
# reporting a whole category cannot pass unnoticed. Generated: do not
# hand-edit. Refresh with:
#     cd transpile && UPDATE_DIAGNOSTICS_BUDGET=1 cargo test --test diagnostics_budget

# What the declared std surface itself reports, which is the same in
# every crate's run. The README's "Engine gaps the stubs expose" says
# what these are; anything else is a stub that stopped resolving.
[surface]
total = 66

[proto]
total = 203

[proto.by_cause]
"obligation deferred: X (the subject is still a type parameter)" = 52
"X's X prints under Debug as whatever its X says, because X's TypeScript is written by hand, so it has no emitted X to call" = 45
"no field X on X" = 14
"method X resolved through trait X, which is not in scope here" = 12
"X is guessed to be a constructor from its capitalisation" = 11
"X does not name a value here" = 10
"this closure's parameter is X typed by nothing the engine can read: neither an annotation on the closure nor the position it stands in says what it holds" = 9
"X is bound here but the engine could not type it" = 8
"X is lowered as a X without the engine having named what it tests" = 7
"X on X is ambiguous between <Vec<u8> as TryInto>::try_into and <T as TryInto>::try_into" = 6
"X converts the error from X to X through X, and the conversion is fixed by a bound on a type parameter, so which impl runs is decided per instantiation and one emitted body cannot say" = 3
"X does not name a function here" = 3
"X has no expected type here, so what it converts to is not said and the value stands as it is" = 3
"no method X on X" = 3
"X and an earlier impl in this file both emit a module-level function called X, so one of them is lost: the two are written for the same type through a reference and without one, and emission erases the reference, so they are one function here" = 2
"X is declared in another module, so this impl is written as module-level functions here rather than as methods on its class" = 2
"X's X prints under Debug as whatever its X says, because X is a std type with no Debug rendering in the port" = 2
"X: X has no JSON spelling here: a JSON object's keys are strings and serde writes a map that way only when the key does" = 2
"X between X and X resolves through X, and no impl in the table performs it" = 1
"X between X and X resolves through X, and the impl is the declared surface's, so the comparison is the runtime's own and X supplies none" = 1
"X has a type the engine cannot say owns anything, so nothing releases it" = 1
"X is printed with X, and the port's X has no alternate form, so it is printed the ordinary way" = 1
"X's X prints under Debug as whatever its X says, because X has no Debug rendering in the port" = 1
"X: X has no JSON spelling in the port, so the type it is a field of gets no X/X pair" = 1
"no declaration for type X" = 1
"the placeholder for the next argument asks for the X formatting trait, which the port does not carry, so the value is written without it" = 1
"the runtime writes this call as something whose result is not the value Rust returns, so the value the statement threw away is not released" = 1

[ankql]
total = 300

[ankql.by_cause]
"X is bound here but the engine could not type it" = 78
"the runtime writes this call as something whose result is not the value Rust returns, so the value the statement threw away is not released" = 51
"X does not name a function here" = 49
"obligation deferred: X (the subject is still a type parameter)" = 42
"X has no expected type here, so what it converts to is not said and the value stands as it is" = 11
"X is written on a receiver the engine could not type, so the copy is written by the method's name alone" = 7
"method X resolved through trait X, which is not in scope here" = 7
"no method X on X" = 7
"X is declared in another module, so this impl is written as module-level functions here rather than as methods on its class" = 6
"X has a type the engine cannot say owns anything, so nothing releases it" = 5
"X is lowered as a X without the engine having named what it tests" = 5
"X converts the error from X to X through X, and the conversion is fixed by a bound on a type parameter, so which impl runs is decided per instantiation and one emitted body cannot say" = 4
"X does not dereference" = 4
"X takes X by X, and JavaScript passes a X by value, so what this function writes to it the caller never sees" = 4
"the alternatives of this pattern bind their names from different places, which the translator cannot write as one test, so the arm is written as one that never matches" = 4
"X and an earlier impl in this file both emit a module-level function called X, so one of them is lost: the two impls differ in something the naming scheme does not write down" = 2
"X dereferences without a field to assign through" = 2
"X's X is encoded by calling X, and nothing in the port emits a class of that name, so reading one raises at run time" = 2
"X's X prints under Debug as whatever its X says, because X is a std type with no Debug rendering in the port" = 2
"an arm of this match names no variant, and the arms it stands for cannot be listed because the engine could not type the subject, so it is not written and the emitted match is not exhaustive" = 2
"X's X prints under Debug as whatever its X says, because X is not declared" = 1
"X's message prints X with X, and it prints as whatever its X says, because X is not declared" = 1
"a second arm names X, and the runtime's match dispatches on the variant alone, so only the first of them can run" = 1
"no declaration for type X" = 1
"this arm tests inside the payload of X, and the runtime's match dispatches on the variant alone with no later arm to fall through to, so the inner test is not made and the arm runs for every X" = 1
"this closure's parameters are X, X typed by nothing the engine can read: neither an annotation on the closure nor the position it stands in says what they hold" = 1

[signals]
total = 265

[signals.by_cause]
"X is bound here but the engine could not type it" = 128
"X does not name a function here" = 28
"this closure takes X by value and the engine could not type it, so it is left out of what the closure owns" = 17
"obligation deferred: X (the subject is still a type parameter)" = 11
"X has a type the engine cannot say owns anything, so nothing releases it" = 9
"this X takes a droppable value apart, and the parts are released separately in Rust" = 9
"this closure owns X and is written as an X, so it is invoked as X and released by whoever holds it" = 9
"the runtime writes this call as something whose result is not the value Rust returns, so the value the statement threw away is not released" = 7
"this closure's parameter is X typed by nothing the engine can read: neither an annotation on the closure nor the position it stands in says what it holds" = 7
"X is guessed to be a constructor from its capitalisation" = 6
"path qualifiers std, sync, atomic are dropped by name" = 6
"X is written on a value the engine could not type, so which conversion it performs is not known" = 5
"path qualifiers std, sync are dropped by name" = 5
"this closure owns X, X and is written as an X, so it is invoked as X and released by whoever holds it" = 4
"X between X and X resolves through X, and no impl in the table performs it" = 3
"path qualifiers std, marker are dropped by name" = 3
"no method X on X" = 2
"the callee is not a path, so nothing names what it calls" = 2
"X does not name a value here" = 1
"method X resolved through trait X, which is not in scope here" = 1
"path qualifiers std are dropped by name" = 1
"raw pointer type is not modelled" = 1

[core]
total = 2527

[core.by_cause]
"X is bound here but the engine could not type it" = 728
"obligation deferred: X (the subject is still a type parameter)" = 428
"X does not name a function here" = 224
"method X resolved through trait X, which is not in scope here" = 87
"no method X on X" = 85
"the runtime writes this call as something whose result is not the value Rust returns, so the value the statement threw away is not released" = 63
"X has a type the engine cannot say owns anything, so nothing releases it" = 58
"X does not name a value here" = 52
"X converts the error from X to X through X, and the conversion is fixed by a bound on a type parameter, so which impl runs is decided per instantiation and one emitted body cannot say" = 51
"this closure's parameter is X typed by nothing the engine can read: neither an annotation on the closure nor the position it stands in says what it holds" = 45
"X is written on a value the engine could not type, so which conversion it performs is not known" = 40
"X's X prints under Debug as whatever its X says, because X is a std type with no Debug rendering in the port" = 34
"path qualifiers std are dropped by name" = 32
"X converts the error from X to X through X, and the impl performing it is the declared surface's, so the conversion is the runtime's own, and the port has no X for it to be a member of" = 31
"no field X on X" = 31
"X has no expected type here, so what it converts to is not said and the value stands as it is" = 28
"the range types in X are not declared, so X is written as X" = 27
"X is lowered as a X without the engine having named what it tests" = 23
"X is declared in another module, so this impl is written as module-level functions here rather than as methods on its class" = 21
"X between X and X resolves through X, and no impl in the table performs it" = 20
"X is guessed to be a constructor from its capitalisation" = 20
"the receiver of X is a projection the impl table could not settle, so the call is written from its name alone" = 19
"the engine could not say what awaiting this produces, so the value the statement threw away is not released" = 16
"this X takes a droppable value apart, and the parts are released separately in Rust" = 16
"X between X and X resolves through X, and the impl is the declared surface's, so the comparison is the runtime's own and X supplies none" = 15
"path qualifiers std, sync are dropped by name" = 14
"this pattern takes the payload out of the value it tests, and the wrapper it came out of is not marked moved, so nothing releases the rest of it on the path that matched" = 14
"X has no hook, so it is emitted as a comment and what it stood for does not run" = 12
"X is printed with X, and it prints as whatever its X says, because X is a std type with no Debug rendering in the port" = 12
"X converts the error from X to X through X, and more than one X impl for X is emitted as X, so the call could not say which" = 11
"X's X prints under Debug as whatever its X says, because X has no Debug rendering in the port" = 11
"no X impl reaches X with the index written here" = 11
"no arm of this match has a type the engine could read" = 11
"X carries a pattern that has to be tested, and the port reads a X's payload once, into the binding, so what it tests is not tested" = 10
"no declaration for type X" = 9
"X grows or shrinks a byte buffer, which a Uint8Array cannot do" = 8
"X has no declared type" = 8
"X is declared with 1 type argument(s) but 2 written here" = 8
"no associated function X on X" = 8
"path qualifiers std, sync, atomic are dropped by name" = 8
"this loop takes the sequence by value, and the runtime does not write it as an array" = 8
"X moves a field the runtime writes as a plain value" = 7
"this X is written as a JSON literal rather than as one expression, and the hook reads the expression form only, so the value is X" = 7
"X converts X to X, which is not a conversion between two numbers" = 6
"X converts from X to X through X, and more than one X impl for X is emitted as X, so the call could not say which" = 6
"X converts from X to X through X, and the impl performing it is the declared surface's, so the conversion is the runtime's own, and the port has no X for it to be a member of" = 6
"X is declared with 1 type argument(s) but 0 written here" = 6
"X and an earlier impl in this file both emit a module-level function called X, so one of them is lost: the two are written for the same type through a reference and without one, and emission erases the reference, so they are one function here" = 5
"X is a receiver the engine does not model" = 5
"this arm tests inside the payload of X, and the runtime's match dispatches on the variant alone with no later arm to fall through to, so the inner test is not made and the arm runs for every X" = 5
"this closure owns X and is written as an X, so it is invoked as X and released by whoever holds it" = 5
"X converts from X to X through X, and no impl in the table performs it" = 4
"X converts from X to X through X, and the conversion is fixed by a bound on a type parameter, so which impl runs is decided per instantiation and one emitted body cannot say" = 4
"X does not dereference" = 4
"X expressions are not typed yet" = 4
"X is written on a receiver the engine could not type, so the copy is written by the method's name alone" = 4
"X on X is ambiguous between <Vec<u8> as TryInto>::try_into and <T as TryInto>::try_into" = 4
"X's X is encoded by calling X, and nothing in the port emits a class of that name, so reading one raises at run time" = 4
"X's X prints under Debug as whatever its X says, because XIX has no Debug rendering in the port" = 4
"X's X prints under Debug as whatever its X says, because XTX has no Debug rendering in the port" = 4
"no declaration for trait X" = 4
"this closure owns X, X and is written as an X, so it is invoked as X and released by whoever holds it" = 4
"X between X" = 3
"X dereferences without a field to assign through" = 3
"X is written without a type with a X of its own to read itself out of the parsed value, so the parse stands alone" = 3
"X's X prints under Debug as whatever its X says, because XRX has no Debug rendering in the port" = 3
"path qualifiers std, collections are dropped by name" = 3
"path qualifiers std, marker are dropped by name" = 3
"X is emitted as a comment and is handed X, which nothing then releases" = 2
"X is printed with X, and it prints as whatever its X says, because the engine could not type it" = 2
"X is printed with X, and the port's X has no alternate form, so it is printed the ordinary way" = 2
"X on X is ambiguous between <&str as TryInto>::try_into and <T as TryInto>::try_into" = 2
"X's X prints under Debug as whatever its X says, because XEX has no Debug rendering in the port" = 2
"X's X prints under Debug as whatever its X says, because XIdX has no Debug rendering in the port" = 2
"an arm of this X match has a guard, and reading the payload to test it takes the wrapper apart, so the arm below it cannot be tried" = 2
"const generic argument is not modelled outside an array length" = 2
"path qualifiers core are dropped by name" = 2
"the alternatives of this pattern bind their names from different places, which the translator cannot write as one test, so the arm is written as one that never matches" = 2
"this closure hands X away when it runs, so it is not the closure's to drop afterwards" = 2
"this closure takes X by value and the engine could not type it, so it is left out of what the closure owns" = 2
"this expression's value is wanted here, and an arm of it leaves the loop around it — the value is computed inside a function, which a X or a X cannot leave, so the jump is written where it does not run" = 2
"this operand takes a value of its own and leaves the function or awaits, so what it took is declared before the short circuit that decides whether the operand runs at all" = 2
"X casts a JavaScript value to a type the emitted call does not name, so the value stands as it is and nothing checks it" = 1
"X converts X to X through X, and the impl performing it is the declared surface's, so the conversion is the runtime's own, and the port has no X for it to be a member of" = 1
"X here tests an X inside a function returning a X, which Rust does not allow, so one of the two types is not what the engine read" = 1
"X is declared with 2 type argument(s) but 0 written here" = 1
"X is emitted as a module-level function here and in X, and the import map is keyed by the name alone, so every call to either reaches one of them" = 1
"X is lowered to the runtime's arbiter, but an arm breaks out of the loop, which an arrow function cannot carry — the exit would land on the arrow function rather than where the source wrote it — so the arbitration is written as statements and the select produces no value" = 1
"X is written without saying which type reads itself out of the bytes, so the reader stands alone" = 1
"X names the type it tests in a turbofish, which emission does not keep, so the test cannot be written" = 1
"X on X is ambiguous between <S as TryStreamExt>::map_err and <T as TryFutureExt>::map_err" = 1
"X on X is ambiguous between <Vec<T> as Extend>::extend and <Vec<T> as Extend>::extend" = 1
"X's X prints under Debug as whatever its X says, because X is not declared" = 1
"X's X prints under Debug as whatever its X says, because XCDX has no Debug rendering in the port" = 1
"X's X prints under Debug as whatever its X says, because XEvX has no Debug rendering in the port" = 1
"X's X prints under Debug as whatever its X says, because XEventX has no Debug rendering in the port" = 1
"X's derived X has no value for this field, because X" = 1
"a second arm names X, and the runtime's match dispatches on the variant alone, so only the first of them can run" = 1
"an arm of this match names no variant, and the arms it stands for cannot be listed because the engine could not type the subject, so it is not written and the emitted match is not exhaustive" = 1
"an empty X has no element type, and the position it stands in does not say one" = 1
"path qualifiers iter are dropped by name" = 1
"path qualifiers std, fmt are dropped by name" = 1
"path qualifiers std, iter are dropped by name" = 1
"this closure's parameters are X, X typed by nothing the engine can read: neither an annotation on the closure nor the position it stands in says what they hold" = 1
